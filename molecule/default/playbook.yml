---
- name: Converge
  hosts: all
  tasks:

    - name: Update APT cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
      become: yes
      when: ansible_os_family == 'Debian'

    - name: "Include ansible-role-chrony"
      include_role:
        name: "ansible-role-chrony"
